# Prompt — deck-view-toggle (2026-07-25)

> Saved verbatim per CLAUDE.md §8 (spans ≥3 files when implemented).

phân bộ từ, list danh sách bộ từ ở tất cả các page thêm options display list thay vì là gird như hiện taị
